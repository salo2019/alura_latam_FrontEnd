//logicos
//AND => && /  OR =>  || /  NOT => !

//operador switch declaracion para muchos casos
switch (key) { //en key va la condicion y en cada value estara los valores correspondiente
    case value:
        
        break;

    default:
        break;
}

//Bucles Varias formas
while (condition) {
    
}

//otra. Se hace siempre la primera vez y luego evalua la condicion
do {
    
} while (condition);

//for
for (let index = 0; index < array.length; index++) {
    const element = array[index];
}